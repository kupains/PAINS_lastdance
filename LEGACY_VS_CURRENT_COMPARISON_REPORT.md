# 기존 PDF 보고서와 현재 통합 결과 비교 보고서

- 작성일: 2026-07-30
- 기존 기준 문서: `C:\Users\USER\Downloads\pains_lastdance.pdf`
- 기존 문서 SHA-256:
  `63EAA47CAFC11048995B4FC4188EAF79D91C0535BF011E7D0220EE0B879559D4`
- 기존 문서 작성일: 2026-07-24
- 현재 상세 보고서: [`FINAL_MODEL_REPORT.md`](FINAL_MODEL_REPORT.md)
- 현재 selection 산출물: `experiments/runs/unified_selection/`
- 현재 final 산출물: `experiments/runs/unified_final_2025/`

이 문서는 기존 PDF의 결론을 현재 결과로 덮어쓰지 않는다. 동일한
2025 등판을 기준으로 무엇이 그대로이고, 무엇이 달라졌으며, 성능 차이가
어디에서 발생했는지를 분리해 비교한다. 숫자로 직접 확인된 사실과 그
사실에 근거한 해석·추론도 구분한다.

## 1. 요약 결론

표면적인 2025 범주 정확도는 기존 PDF보다 낮아졌다.

- XGBoost: 51.71% → 50.10%, `-1.61%p`
- Ridge: 52.92% → 49.90%, `-3.02%p`
- EWMA4: 52.31% → 49.09%, `-3.22%p`

따라서 “현재 모델의 2025 범주 정확도가 기존 보고서보다 좋아졌다”라고
말할 수 없다. 현재 common 497행에서 가장 높은 모델도 XGBoost 50.10%이며,
EWMA4보다 1.01%p 높은 정도다.

다만 이 하락을 곧바로 연속 Stuff+ 예측력의 악화로 해석하는 것도
정확하지 않다. 양쪽의 497등판은 완전히 같고 연속 Stuff+ 정답도 사실상
같지만, 범주 경계를 계산하는 학습 표본이 바뀌었다. 기존 코드는 모든
후보 피처가 완전한 2,058행만으로 q33/q67을 계산했고, 현재 코드는
피처 결측 여부와 무관한 2,387개 유효 target 등판으로 q33/q67을 계산한다.
그 결과 2025 정답 범주가 497건 중 24건, 4.83% 바뀌었다.

같은 범주 정의로 다시 비교하는 진단에서는 결과가 다르게 보인다.

- 현재 범주 정의로 기존 예측을 다시 분류하면 XGBoost 47.89%,
  Ridge 49.70%, EWMA4 49.70%다.
- 같은 현재 범주 정의에서 현재 예측은 XGBoost 50.10%,
  Ridge 49.90%, EWMA4 49.09%다.
- 즉 현재 정의 안에서는 XGBoost가 기존 연속 예측보다 2.21%p,
  Ridge가 0.20%p 높고, EWMA4는 0.60%p 낮다.
- 동일 497등판의 연속 Stuff+ MAE와 RMSE는 세 모델 모두 현재 실행이
  소폭 낮다.

이 재분류는 원인을 이해하기 위한 사후 진단이지 새로운 모델 선택
근거가 아니다. 가장 타당한 결론은 다음과 같다.

1. 직접 보고된 범주 성능은 실제로 낮아졌다.
2. 하락의 가장 큰 확인 가능 원인은 모델 자체보다 범주 경계 모집단의
   수정이다.
3. 현재 파이프라인은 실행 절차, 누출 방지, 전체 560행 coverage,
   TCN·ordinal 검증 면에서 더 엄밀하지만, 그 엄밀성이 2025 범주
   정확도 향상을 보장하지는 않았다.
4. TCN과 ordinal은 실제로 학습·평가됐지만 기존 모델을 대체할 정도의
   검증 우위를 내지 못했다.

## 2. 비교 자료와 재현 기준

### 2.1 기존 결과

기존 PDF의 핵심 수치는 다음 페이지에서 확인했다.

- p.1: XGBoost와 Ridge의 2025 최종 성능
- p.2~7: 2020~2024 학습, 2022~2024 순차 검증과 선택 규칙
- p.8~10: EWMA4 포함 최종 성능, 혼동행렬, 모델 일치 진단
- p.11: 투수별 결과
- p.12~13: 표본 선택과 연구 절차의 한계
- p.14~15: 산출물과 결론

PDF 수치의 행 단위 재현에는 다음 파일을 사용했다.

- XGBoost:
  `experiments/runs/legacy_snapshot_xgboost/locked_xgboost_2025_predictions.parquet`
- Ridge:
  `experiments/runs/legacy_snapshot_xgboost/ridge_benchmark_2025_predictions.parquet`
- EWMA4:
  `experiments/runs/legacy_snapshot_reproduction/predictions.parquet`의
  `model=ewma_span4`
- 기존 XGBoost 설정:
  `experiments/runs/legacy_snapshot_xgboost/locked_xgboost_config.json`
- 기존 코드:
  `experiments/runs/legacy_snapshot_source/`

### 2.2 현재 결과

현재 결과는 다음 파일을 기준으로 했다.

- 설정 확정:
  `experiments/runs/unified_selection/locked_model_config.json`
- 검증 요약:
  `experiments/runs/unified_selection/validation_metrics_summary.csv`
- 최종 예측:
  `experiments/runs/unified_final_2025/final_2025_predictions.parquet`
- common 성능:
  `experiments/runs/unified_final_2025/final_2025_common_sample_metrics.csv`
- deployable 성능:
  `experiments/runs/unified_final_2025/final_2025_deployable_metrics.csv`
- 최종 실행 메타데이터:
  `experiments/runs/unified_final_2025/final_report.json`

현재 설정 파일에 기록된 식별자는 다음과 같다.

- dataset fingerprint:
  `db4cca7577b0758ca1f1e3b929f68a6186a47383a8cb1866aef482eb13695604`
- fold manifest fingerprint:
  `347c4845323204d7ccc12d0212f9844a948ea9aad6b04c7ac9d525dd913f550d`
- 실행 당시 code version:
  `7eb80cd95ebf13480c8a38dbbe74a5af752bf58c+analysis-sha256:c11fd0ae3f34f4b8b5db0422eeb4c9fef23f44e2e12ee0fb0fe31344d58ba053`

### 2.3 동일 표본 확인

기존 497행과 현재 Ridge/XGBoost가 예측 가능한 497행을
`pitcher + game_date`로 대조한 결과:

- 양쪽 행 수: 각각 497
- 양쪽에 모두 있는 행: 497
- 기존에만 있는 행: 0
- 현재에만 있는 행: 0
- 연속 `target_y` 최대 절대 차이: `3.81e-6`

마지막 차이는 저장·집계 과정의 부동소수점 정밀도 차이이며, 실제 비교
대상이 달라졌다고 볼 크기가 아니다. 즉 최종 정확도 차이는 2025 경기
표본이 달라서 생긴 것이 아니다.

## 3. 최종 성능 직접 비교

### 3.1 동일 497행의 보고 수치

아래 표는 각 실행이 당시 사용한 자체 q33/q67과 자체 정답 범주로 계산한
공식 수치다. 따라서 “각 보고서에 실제로 적힌 결과”의 비교에는 맞지만,
범주 정의가 달라 완전히 동일한 scoring rule 비교는 아니다.

| 모델 | 실행 | Accuracy | Balanced accuracy | Macro-F1 | Ordinal MAE |
|---|---|---:|---:|---:|---:|
| EWMA4 | 기존 PDF | 0.523139 | 0.506463 | 0.509693 | 0.545272 |
| EWMA4 | 현재 | 0.490946 | 0.474060 | 0.478178 | 0.569416 |
| EWMA4 | 현재−기존 | **-0.032193** | **-0.032403** | **-0.031515** | **+0.024145** |
| Ridge | 기존 PDF | 0.529175 | 0.510519 | 0.513983 | 0.531187 |
| Ridge | 현재 | 0.498994 | 0.482530 | 0.487167 | 0.557344 |
| Ridge | 현재−기존 | **-0.030181** | **-0.027988** | **-0.026816** | **+0.026157** |
| XGBoost | 기존 PDF | 0.517103 | 0.493113 | 0.495514 | 0.547284 |
| XGBoost | 현재 | 0.501006 | 0.484720 | 0.489101 | 0.559356 |
| XGBoost | 현재−기존 | **-0.016097** | **-0.008393** | **-0.006413** | **+0.012072** |

정확도 기준으로는 기존 Ridge가 전체 표에서 가장 높다. 현재 common
표에서는 XGBoost가 가장 높지만 Ridge와의 차이는 한 경기,
`1 / 497 = 0.20%p`뿐이다.

### 3.2 현재에 추가된 세 출력

기존 PDF에는 ordinal, TCN, Ridge–TCN ensemble 최종 결과가 없다.

| 현재 추가 출력 | Common N | Common accuracy | Common BA | Deployable N | Coverage | Deployable accuracy |
|---|---:|---:|---:|---:|---:|---:|
| Ordinal linear | 497 | 0.474849 | 0.461470 | 560 | 1.0000 | 0.487500 |
| TCN H1-4 | 497 | 0.498994 | 0.482675 | 560 | 1.0000 | 0.512500 |
| Ridge–TCN, beta=0 | 497 | 0.498994 | 0.482530 | 497 | 0.8875 | 0.498994 |

TCN의 51.25%는 전체 560행 결과이고 기존 PDF의 497행 결과와 직접 비교할
수 없다. 동일 497행에서 TCN은 49.90%다. 앙상블 beta는 검증 단계에서
0으로 최종 확정됐으므로 출력이 Ridge와 같은 것은 계산 오류가 아니다.

## 4. 범주 경계 변경의 정량 영향

### 4.1 기존 코드와 현재 코드의 차이

기존 `stuff_mlb_temporal_final.py`는 먼저 `_complete_rows()`로
`FEATURES` 28개 전체, target과 EWMA가 모두 완전한 행만 남긴 뒤 그 행의
`target_y`로 q33/q67을 계산했다. compact16 모델이어도 사용하지 않는
12개 후보 피처의 결측이 경계 계산 표본까지 줄였다.

현재 `lib/stuff_experiment.py`는 다음 순서로 처리한다.

1. 공식 선발, 50구 이상, Stuff+ 존재 조건을 만족하는 모든
   `target_eligible` 학습행으로 q33/q67을 계산한다.
2. 그 후 각 모델이 실제 사용하는 피처에 따라 학습 가능 행을 정한다.
3. 범주 정의와 특정 모델의 피처 결측을 분리한다.

최종 2025 경계의 학습행 수:

| 구분 | 2020 | 2021 | 2022 | 2023 | 2024 | 합계 |
|---|---:|---:|---:|---:|---:|---:|
| 기존 q 경계: 28피처 complete-case | 118 | 436 | 482 | 520 | 502 | 2,058 |
| 현재 q 경계: 전체 target-eligible | 176 | 512 | 546 | 583 | 570 | 2,387 |
| 차이 | +58 | +76 | +64 | +63 | +68 | **+329** |

현재 경계 표본은 기존보다 329행, 기존 표본 대비 약 16.0% 많다. 현재
compact16 Ridge/XGBoost의 실제 최종 학습 가능 행은 2,070행이지만,
경계는 모델 피처 결측과 독립적으로 2,387행 전체에서 계산된다.

현재 manifest를 진단 목적으로 다시 28피처 complete-case로 제한하면
기존 경계와의 투수별 평균 절대 차이는 q33 0.0301, q67 0.0456까지
줄고, q33은 11 / 19명, q67은 13 / 19명에서 정확히 일치한다. 남은 작은
차이는 50구 미만 history 보존과 시즌 경계 처리 변경에서 발생한다.
이는 전체 경계 차이의 주된 원인이 complete-case 종속 여부라는 추가
근거다.

### 4.2 q33/q67과 정답 범주 변화

19명 투수별 기존 경계와 현재 경계를 대조한 결과:

- q33이 정확히 같은 투수: 2 / 19
- q67이 정확히 같은 투수: 1 / 19
- 투수별 q33 평균 절대 변화: 0.3965 Stuff+ point
- 투수별 q67 평균 절대 변화: 0.3428 Stuff+ point
- q33 상승/동일/하락: 8 / 2 / 9명
- q67 상승/동일/하락: 7 / 1 / 11명

변화 방향이 한쪽으로만 치우치지는 않았다. 따라서 현재 경계가 항상 더
엄격하거나 항상 더 느슨해진 것이 아니라, 피처 결측으로 빠졌던 경기의
분포가 투수마다 달랐다고 해석해야 한다.

같은 2025 `target_y`를 각각의 경계로 분류한 결과 24 / 497행이
다른 정답 범주를 갖는다.

| 기존 정답＼현재 정답 | Low | Middle | High | 합계 |
|---|---:|---:|---:|---:|
| Low | 203 | 10 | 0 | 213 |
| Middle | 4 | 150 | 7 | 161 |
| High | 0 | 3 | 120 | 123 |
| 합계 | 207 | 163 | 127 | 497 |

24건은 전체의 4.83%다. 단계가 두 칸 바뀐 Low↔High 정답은 없고, 모두
인접 범주 이동이다. 변화가 많은 투수는 Framber Valdez 4건,
Mitch Keller 4건, Patrick Corbin 3건이다.

### 4.3 같은 규칙으로 다시 분류한 고정 경로 진단

아래는 연속 예측을 다른 q33/q67으로 다시 분류해 scoring rule 효과와
예측값 변경 효과를 순서대로 본 진단이다.

| 모델 | 기존 예측·기존 경계/정답 | 기존 예측·현재 경계/정답 | 현재 예측·현재 경계/정답 |
|---|---:|---:|---:|
| EWMA4 Accuracy | 0.523139 | 0.496982 | 0.490946 |
| EWMA4 BA | 0.506463 | 0.481934 | 0.474060 |
| Ridge Accuracy | 0.529175 | 0.496982 | 0.498994 |
| Ridge BA | 0.510519 | 0.479326 | 0.482530 |
| XGBoost Accuracy | 0.517103 | 0.478873 | 0.501006 |
| XGBoost BA | 0.493113 | 0.458892 | 0.484720 |

이 경로에서 경계와 정답 정의만 현재 방식으로 바꿨을 때의 accuracy
변화는 EWMA4 `-2.62%p`, Ridge `-3.22%p`, XGBoost `-3.82%p`다.
그 상태에서 현재 연속 예측으로 바꿨을 때 EWMA4는 추가 `-0.60%p`,
Ridge는 `+0.20%p`, XGBoost는 `+2.21%p`다.

분해 순서를 반대로 잡으면 기여량은 달라질 수 있으므로 이 표를 엄밀한
인과 분해로 부르지는 않는다. 그러나 세 모델 모두에서 범주 경계 변경이
큰 영향을 주고, 현재 Ridge/XGBoost 예측 자체가 현재 scoring rule에서
기존 예측보다 나쁘지 않다는 점은 직접 확인된다.

고정된 하나의 범주 정의 안에서 기존 점수와 현재 점수의 정답 여부를
paired exact test로 비교해도 통계적으로 확정적인 차이는 나오지 않았다.
현재 경계 기준 p-value는 EWMA4 0.453, Ridge 1.000, XGBoost 0.099다.
기존 경계 기준도 모두 0.4보다 크다. 원래 보고 수치끼리는 정답 label
자체가 다르므로 paired significance test를 적용하지 않았다. 따라서
현재 XGBoost의 `+2.21%p`도 유망한 진단 차이이지, 497행만으로 입증된
일반화 우위라고 표현하지 않는다.

## 5. 연속 Stuff+ 예측은 악화됐는가

같은 497행에서 범주화 전 연속 Stuff+ 예측 오차를 계산했다.

| 모델 | 실행 | Stuff+ MAE | Stuff+ RMSE |
|---|---|---:|---:|
| EWMA4 | 기존 | 4.3736 | 5.7218 |
| EWMA4 | 현재 | **4.3632** | **5.7098** |
| Ridge | 기존 | 4.3684 | 5.6970 |
| Ridge | 현재 | **4.3568** | **5.6883** |
| XGBoost | 기존 | 4.4600 | 5.7779 |
| XGBoost | 현재 | **4.3802** | **5.7050** |

세 모델 모두 현재 오차가 소폭 낮다. 개선 폭은 작지만, 적어도 범주
정확도 하락을 “연속 Stuff+를 더 못 맞히게 됐다”라고 단정할 근거는 없다.
이번 차이는 연속 예측과 경계 기반 분류 지표가 서로 다른 정보를 준다는
사례다.

현재 TCN은 동일 497행에서 Stuff+ MAE 4.3415, RMSE 5.6774로 현재 연속
예측 중 가장 낮지만, 범주 정확도는 49.90%로 XGBoost 50.10%보다 낮다.
경계 근처의 작은 연속 오차 변화가 범주 정답 수와 일대일로 대응하지
않기 때문이다.

## 6. 데이터 이력과 모델 설정의 변경

### 6.1 50구 미만 공식 선발과 시즌 경계

기존 파이프라인은 `pitch_count >= 50`을 먼저 적용한 뒤 과거 이력을
계산했다. 50구 미만 공식 선발은 다음 경기의 history에서도 사라졌고,
rest·rolling history는 투수와 시즌별로 다시 시작했다.

현재 파이프라인은 모든 공식 선발을 시간 이력에 남기고 50구 이상 조건은
예측 target에만 적용한다. 또한 투수별 시간축을 시즌 사이에도 이어
계산하고 `season_start`, `long_gap`, `rest_days_log`로 긴 공백을 표시한다.

현재 데이터에서 Stuff+가 존재하지만 target에는 부적격인 50구 미만
공식 선발은 2020~2025에 45행, 2025에 8행이다. 같은 497행의 기존·현재
EWMA4 차이가 0.01을 넘는 38행은 모두 2025년 50구 미만 선발 뒤에
발생했다. 따라서 EWMA4의 작은 추가 차이는 의도한 history 보존 변경으로
설명된다.

### 6.2 Ridge 설정

| 항목 | 기존 PDF | 현재 |
|---|---:|---:|
| EWMA span | 4 | 4 |
| 피처 | compact16 | compact16 |
| Ridge alpha | 100 | 100 |
| correction scale | 0.50 | 0.25 |

현재 2022~2024 선택 점수는 scale 0.25가 0.541954,
scale 0.50이 0.534619, scale 1.00이 0.531637이었다. 따라서 scale 0.25는
2025를 보고 낮춘 값이 아니라 현재 rolling validation에서 선택된 값이다.

### 6.3 XGBoost 설정

| 항목 | 기존 PDF | 현재 |
|---|---:|---:|
| trees | 100 | 100 |
| max depth | 2 | 2 |
| learning rate | 0.042206 | 0.066101 |
| min child weight | 15.0427 | 13.9429 |
| L2 | 16.8233 | 29.1418 |
| L1 | 0.7059 | 0.6153 |
| subsample | 0.9997 | 0.6826 |
| column sample | 0.5856 | 0.7955 |
| correction scale | 0.575009 | 0.221845 |

현재 Ridge와 XGBoost는 기존보다 EWMA4 보정을 작게 반영한다. 동일
497행에서 `|최종 예측−EWMA4|` 평균은 Ridge가 0.4576에서 0.2321,
XGBoost가 0.8018에서 0.3241로 줄었다. 이는 현재 예측이 더 보수적이고
모델끼리 더 자주 일치하는 이유를 설명한다.

## 7. 모델 선택 결과 비교

각 실행 내부에서 계산한 2022~2024 validation 결과다. 범주 경계와
학습행 정책이 다르므로 절대값을 동일 실험처럼 비교하면 안 되지만,
각 실행의 validation과 자체 2025 사이 간격은 확인할 수 있다.

| 모델 | 실행 | Validation Mean BA | Std | Selection score | 2025 BA | Validation→2025 |
|---|---|---:|---:|---:|---:|---:|
| EWMA4 | 기존 PDF | 0.5410 | 0.0155 | 0.5333 | 0.5065 | -0.0345 |
| EWMA4 | 현재 | 0.5583 | 0.0231 | 0.5467 | 0.4741 | -0.0842 |
| Ridge | 기존 PDF | 0.5404 | 0.0123 | 0.5343 | 0.5105 | -0.0299 |
| Ridge | 현재 | 0.5539 | 0.0238 | 0.5420 | 0.4825 | -0.0713 |
| XGBoost | 기존 PDF | 0.5456 | 0.0094 | 0.5409 | 0.4931 | -0.0525 |
| XGBoost | 현재 | 0.5613 | 0.0177 | 0.5525 | 0.4847 | -0.0766 |

현재 실행은 validation BA가 더 높지만 2025 BA는 낮고, validation에서
2025로 내려가는 폭도 더 크다. 확인 가능한 사실은 여기까지다. 가능한
해석은 2025가 현재 target 정의에서 개발 연도보다 더 어려운 분포였거나,
세 개 validation year에 대한 선택 변동이 한 시즌 final에 그대로
일반화되지 않았다는 것이다. 한 번의 2025 final만으로 “과적합”과
“실제 regime shift”를 분리해 확정할 수는 없다.

## 8. 신규 모델이 최종 정확도를 높이지 못한 이유

### 8.1 TCN

선택된 TCN H1-4의 2022~2024 mean BA는 0.554810으로 Ridge 0.553851보다
조금 높았지만, 변동성을 뺀 selection score는 TCN 0.541679,
Ridge 0.541954였다. 차이가 매우 작고 TCN이 더 복잡하므로 기존 모델
대체 기준을 통과하지 못했다.

Ordered TCN과 shuffled TCN의 점수 차이도 작았다. 이는 최근 8경기의
순서 정보가 전혀 없다는 증명은 아니지만, 이 표본에서는 시간 순서를
추가한 이득이 안정적으로 크지 않았다는 뜻이다.

2025에서는 TCN이 전체 560행을 예측하고 연속 오차도 가장 낮았지만,
common 497행 범주 정확도는 Ridge와 같은 49.90%였다. 새로운 구조를
추가했다는 사실 자체가 분류 정확도 개선을 보장하지 않았다.

### 8.2 Ordinal distance

거리 손실은 누락된 것이 아니라 구현하고 평가했다.

| Ordinal 후보 | Mean BA | Std | Score | Ordinal MAE | 평균 extreme errors |
|---|---:|---:|---:|---:|---:|
| distance=0 | **0.473438** | 0.044491 | **0.451193** | 0.645967 | 80.33 |
| distance=0.25 | 0.465934 | **0.038318** | 0.446775 | **0.634932** | **66.67** |

distance=0.25는 거리 오차와 극단 오류를 줄였지만, 사전에 정한 주 선택
점수는 더 낮았다. 따라서 distance=0이 최종 설정으로 확정됐다. 이는
“거리 개념을 고려하지 않았다”가 아니라 “거리 지표 개선과 balanced
accuracy 사이의 trade-off에서 사전 선택 규칙을 따랐다”는 뜻이다.

### 8.3 Ensemble

Ridge–TCN beta=0.3의 raw selection score는 0.544192로 beta=0의
0.541954보다 높았다. 그러나 차이 0.002238이 사전 simplicity margin
0.003 안이어서 더 단순한 beta=0을 최종 확정했다. 따라서 final ensemble은
Ridge와 동일하며, 추가 정확도도 없다.

## 9. 모델 일치 진단 비교

| 진단 | 기존 XGBoost–Ridge | 현재 XGBoost–Ridge |
|---|---:|---:|
| 공통 행 | 497 | 497 |
| 같은 범주 | 455 | 480 |
| 일치율 | 91.55% | 96.58% |
| 일치 행 정확도 | 53.41% | 50.21% |
| 다른 범주 | 42 | 17 |
| 불일치 행 XGBoost 정확도 | 33.33% | 47.06% |
| 불일치 행 Ridge 정확도 | 47.62% | 41.18% |

현재 두 모델의 보정 크기가 작아져 일치율은 높아졌지만, 일치한 예측의
정확도는 낮아졌다. 높은 agreement는 안정성 또는 유사성을 뜻할 수
있지만, agreement 자체가 정답률을 보장하지는 않는다.

## 10. 방법론과 산출물 비교

| 항목 | 기존 PDF 시점 | 현재 통합 결과 |
|---|---|---|
| selection/final 진입점 | 두 스크립트가 서로 다른 선택 책임을 가짐 | `select_models.py`와 `final_evaluate.py`로 책임 분리 |
| validation fold | XGBoost는 2022~2024, legacy final 선택은 2023~2024 | 모든 후보를 2022~2024 공통 fold로 평가 |
| 2025 final | 기존 final 스크립트가 일부 설정을 다시 선택 | 사전 확정 설정과 fingerprint가 없거나 다르면 실패 |
| q33/q67 모집단 | 전체 28피처 complete-case | 모든 target-eligible 학습행 |
| model complete-case | compact 모델도 전체 후보 28피처 요구 | 실제 설정된 피처만 요구 |
| 50구 미만 선발 | target와 history에서 모두 제거 | target에서는 제외, history에는 보존 |
| 시즌 경계 | history와 rest를 시즌마다 다시 시작 | 투수 시간축을 연결하고 공백 flag 추가 |
| 평가 표본 | 모든 모델 497행만 보고 | common 497과 model별 deployable 560을 분리 |
| 직접 ordinal | 없음 | proportional-odds linear와 distance 후보 |
| sequence model | 없음 | shared causal TCN + pitcher-specific head |
| seed 진단 | 해당 없음 | TCN 5 seeds와 seed 변동성 반영 |
| 산출물 추적 | 여러 결과 디렉터리와 설정 파일 | dataset/fold/code fingerprint와 단일 final schema |

현재 결과물의 가장 분명한 개선은 “더 높은 2025 정확도”가 아니라
실험 계약과 재현성이다. 반대로 현재 결과물의 가장 분명한 미달점은
추가한 복잡도가 common 497행의 범주 정확도를 높이지 못했다는 것이다.

## 11. 원인 판단: 확인된 사실과 추론

### 11.1 직접 확인된 원인

1. **범주 경계 모집단 변경이 가장 큰 수치 영향을 냈다.**
   같은 497행 중 24개 정답 범주가 바뀌었고, 기존 연속 예측을 현재
   경계로만 재분류해도 accuracy가 2.62~3.82%p 낮아졌다.
2. **history 정의가 EWMA와 파생 피처를 바꿨다.**
   50구 미만 공식 선발을 보존하고 시즌 사이 이력을 연결했다.
3. **Ridge와 XGBoost 보정이 더 보수적으로 선택됐다.**
   correction scale이 각각 0.50→0.25, 0.575→0.222로 줄었다.
4. **신규 모델은 검증 대체 기준을 통과하지 못했다.**
   TCN score가 Ridge보다 낮았고 ordinal은 훨씬 낮았다.
5. **2025의 분류 성능은 개발 검증보다 크게 낮았다.**
   현재 세 기존 모델의 BA 하락 폭은 7.1~8.4%p다.

### 11.2 근거가 있지만 확정할 수 없는 추론

1. **2025 분포 변화 또는 threshold 근처 표본의 민감성**
   현재 q 정의에서 2025가 2022~2024보다 어려웠을 가능성이 높다.
   다만 한 시즌만으로 구조적 regime shift라고 확정할 수 없다.
2. **작은 표본에서의 선택 오차**
   투수별 경기 수가 작고 validation year도 세 개뿐이어서 0.1~0.5%p
   수준의 후보 차이는 다음 해에 순서가 바뀔 수 있다.
3. **보수적 correction의 한계**
   작은 correction은 과도한 예측을 줄이지만, EWMA가 틀린 경계 근처
   경기에서 범주를 뒤집을 기회도 줄인다. 현재 Ridge/XGBoost agreement가
   96.58%까지 올라간 사실과 일치하는 해석이지만, 이것만을 정확도 하락의
   단일 원인으로 볼 수는 없다.
4. **복잡한 모델에 충분하지 않은 유효 신호**
   Ordered와 shuffled TCN 차이가 작고 H2가 H1을 안정적으로 이기지
   못했다. 최근 8경기 raw sequence에 EWMA4를 크게 넘어서는 안정적
   신호가 제한적일 가능성이 있다.

## 12. 기존 PDF 자체의 한계와 현재 남은 한계

기존 PDF가 스스로 밝힌 중요한 한계:

- 2025 선발 수를 확인해 19명 평가 roster를 정했다(p.3, p.12).
- 연구자가 초기 탐색에서 2025 결과를 본 이력을 되돌릴 수 없다(p.12).
- Ridge를 보조 모델로 제시한 역할 결정은 첫 XGBoost final 이후
  이루어졌다(p.13).
- 최종 평가는 2025 한 시즌뿐이다(p.12).

현재 통합 실행은 모델·파라미터·ensemble 역할을 2022~2024 결과로
확정하고 final에서 다시 선택하지 않도록 개선했다. 하지만 다음 한계는
남아 있다.

- 기존 연구 roster를 유지했기 때문에 현재 설정에도
  `uses_2025_roster_availability=true`가 기록된다. 2025 결과값은
  선택 점수에 쓰지 않았지만 roster availability는 사용했다.
- compact16 결측으로 Ridge/XGBoost coverage는 88.75%다.
- 2025는 여전히 한 시즌 final이다.
- 현재 결과를 본 뒤 target 정의나 모델 역할을 다시 바꾸면 새로운
  사후 선택 문제가 생긴다.

## 13. 권고

1. **현재 target 정의를 그대로 고정하고 다음 미사용 시즌으로 확인한다.**
   2025 결과만 보고 기존 complete-case 경계로 되돌리면 또 다른 사후
   선택이 된다.
2. **범주 지표와 연속 Stuff+ 오차를 함께 보고한다.**
   이번 사례처럼 연속 오차는 개선돼도 경계 기반 accuracy는 낮아질 수
   있다.
3. **경계 민감도는 모델 선택과 분리된 진단으로 유지한다.**
   전체 target-eligible 경계와 feature-complete 경계 양쪽 결과를
   sensitivity table로 낼 수 있지만, 주 target은 사전에 하나로 정해야 한다.
4. **Common과 deployable을 계속 분리한다.**
   TCN 560행 51.25%와 기존 497행 52.92%를 직접 순위 비교하지 않는다.
5. **신규 모델을 성능 향상으로 과장하지 않는다.**
   TCN은 coverage와 연속 오차 면에서 유용하지만, 현재 common 범주
   정확도 우위는 없다. Ordinal distance도 명확한 trade-off가 있다.
6. **다음 시즌에는 설정을 바꾸지 않은 진짜 forward test를 우선한다.**
   모델 변경 실험은 별도 development branch와 과거 fold에서 수행하고,
   미사용 시즌 결과는 마지막 한 번만 연다.

## 14. 최종 판단

현재 최종 결과물은 기존 PDF보다 실험 절차가 엄밀하고, 모델 범위와
coverage가 넓으며, 설정·데이터·fold를 재현할 수 있다. 그러나 2025
범주 정확도 자체는 기존 PDF보다 낮다. 두 문장은 동시에 참이다.

성능 하락의 주된 확인 가능 원인은 동일 경기의 예측값이 크게 나빠진
것이 아니라, 결측 피처에 종속됐던 기존 범주 경계를 전체 유효 target
기준으로 바로잡으면서 평가 정답 24건이 바뀐 것이다. 현재 정의로
동일하게 재분류하면 현재 XGBoost와 Ridge는 기존 연속 예측보다 각각
2.21%p, 0.20%p 높고, 연속 Stuff+ 오차도 세 기존 모델 모두 소폭 낮다.

그렇더라도 현재 최고 common accuracy가 약 50%라는 사실은 남는다.
따라서 현재 결과를 “예측 성능 개선 완료”로 표현하기보다 “평가 계약과
재현성을 바로잡았고, 추가 모델의 실제 한계까지 확인한 기준선”으로
표현하는 것이 가장 정확하다.
